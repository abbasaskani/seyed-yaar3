name: run (legacy disabled)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'This legacy workflow is intentionally disabled.'
          echo 'Use .github/workflows/run_daily.yml as the only canonical scheduled runner.'
